version: 2.1
jobs:
  build:
    docker:
      - image: 'accipiter7/thy:7.0'
    steps:
      - run:  
          name: Compilation
          command: bash /root/project/c/build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - build
